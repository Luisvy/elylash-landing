# Ely Lash Landing

Angular landing page for Ely Lash Room with routed sections, premium animated navigation, gallery carousel, and Calendly booking integration.

## Scripts

- `npm install`
- `npm start`
- `npm run build`
- `npm test`
